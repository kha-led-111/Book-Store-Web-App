@model IEnumerable<Publishing_House>

@{
	ViewData["title"] = "List of publish house";
}
<div class="row">
	<div class="col-md-8 offset-md-2">
		<table class="table">
			<thead>
				<tr class="text-center">
					<th>@Html.DisplayNameFor(Model => Model.Logo)</th>
					<th>@Html.DisplayNameFor(Model => Model.Fullname)</th>
					<th>@Html.DisplayNameFor(Model => Model.Bio) </th>
					<th>Actions </th>
				</tr>

			</thead>
			<tbody>
				@foreach (var item in Model)
				{
					<tr>
						<td class="align-middle">
							<img class="rounded-circle" src="@item.Logo" style="max-width:180px" />
						</td>
						<td class="align-middle">@Html.DisplayFor(Model => item.Fullname)</td>
						<td class="align-middle">@Html.DisplayFor(Model => item.Bio )</td>
						<td class="align-middle">
							<a class="btn btn-outline-primary" asp-controller="Publishing_House" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Edit</a>|
							<a class="btn btn-outline-info" asp-controller="Publishing_House" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-eye"></i>Details</a>|
							<a class="btn btn-danger" asp-controller="Publishing_House" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete</a>


						</td>
					</tr>
				}
			</tbody>
		</table>


	</div>


</div>
<div style="position:fixed; right:45px; bottom:75px; " class="text-white">
	<a asp-controller="Publishing_House" asp-action="Create" class="btn btn-success"><i class="bi bi-plus-circle"></i>Add New</a>

</div>

